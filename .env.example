AI_HUB_API_KEY=

# Local-only fallback for srv/cpi-deploy-service.js when VCAP_SERVICES has no
# bound "cpi-ai-platform-api" (it-rt) instance. Fill these in from the service
# key you created for the cpi-ai-platform-api instance. Copy this file to .env
# and never commit the copy with real values.
CPI_CLIENT_ID=
CPI_CLIENT_SECRET=
CPI_TOKEN_URL=
CPI_API_BASE_URL=
