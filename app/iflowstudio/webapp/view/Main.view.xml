<mvc:View
    controllerName="iflowstudio.controller.Main"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    displayBlock="true">

    <Page
        id="mainPage"
        showHeader="false"
        class="sapUiResponsiveContentPadding">

        <content>

            <!-- Header -->
            <VBox class="heroSection">

                <HBox
                    justifyContent="SpaceBetween"
                    alignItems="Center"
                    class="topBar">

                    <HBox alignItems="Center">
                        <core:Icon
                            src="sap-icon://process"
                            size="2rem"
                            class="appIcon"/>

                        <VBox class="sapUiSmallMarginBegin">
                            <Title
                                text="iFlow Studio"
                                level="H2"/>

                            <Text
                                text="Build SAP Integration Flows from natural language"/>
                        </VBox>
                    </HBox>

                    <ObjectStatus
                        text="Ready"
                        state="Success"
                        icon="sap-icon://status-positive"/>
                </HBox>


                <!-- Hero -->
                <VBox class="heroContent">

                    <Title
                        text="Create an Integration Flow"
                        level="H1"
                        class="heroTitle"/>

                    <Text
                        text="Describe the integration you need. iFlow Studio will turn your scenario into an SAP Integration Flow."
                        class="heroSubtitle"/>

                </VBox>

            </VBox>


            <!-- Scenario Card -->
            <Panel
                headerText="Describe your scenario"
                class="scenarioCard"
                expandable="false">

                <content>

                    <VBox>

                        <Text
                            text="You don't need to know SAP Integration Suite components. Just describe the business requirement."
                            class="scenarioHint"/>

                        <TextArea
                            id="scenarioInput"
                            width="100%"
                            rows="8"
                            growing="true"
                            maxLength="5000"
                            placeholder="Example:

Receive orders through HTTPS. Validate the order. Route domestic and international orders differently. Enrich international orders and finally send the result to an HTTP service."
                            value="{view>/scenario}"
                            class="scenarioInput"/>


                        <!-- Examples -->
                        <HBox
                            wrap="Wrap"
                            class="exampleRow">

                            <Button
                                text="Order processing"
                                icon="sap-icon://example"
                                type="Transparent"
                                press=".onExamplePress"/>

                            <Button
                                text="Customer synchronization"
                                icon="sap-icon://customer"
                                type="Transparent"
                                press=".onExamplePress"/>

                            <Button
                                text="Invoice integration"
                                icon="sap-icon://receipt"
                                type="Transparent"
                                press=".onExamplePress"/>

                            <Button
                                text="API transformation"
                                icon="sap-icon://process"
                                type="Transparent"
                                press=".onExamplePress"/>

                        </HBox>


                        <!-- Generate -->
                        <HBox
                            justifyContent="End"
                            class="generateRow">

                            <Button
                                id="generateButton"
                                text="Generate Integration Flow"
                                icon="sap-icon://process"
                                type="Emphasized"
                                press=".onGenerateIntegrationFlow"
                                busy="{view>/generating}"
                                enabled="{= !${view>/generating} }"
                                class="generateButton"/>

                        </HBox>

                    </VBox>

                </content>

            </Panel>


            <!-- Generated Flow -->
            <Panel
                id="flowPanel"
                headerText="Generated Integration Flow"
                visible="{view>/generated}"
                class="flowCard">

                <content>

                    <VBox>

                        <HBox
                            justifyContent="SpaceBetween"
                            alignItems="Center"
                            class="flowHeader">

                            <VBox>

                                <Title
                                    text="{view>/flowName}"
                                    level="H3"/>

                                <Text
                                    text="Generated from your business scenario"/>

                            </VBox>

                            <ObjectStatus
                                text="Generated"
                                state="Success"
                                icon="sap-icon://status-positive"/>

                        </HBox>


                        <!-- Flow visualization -->
                        <HBox
                            id="flowVisualization"
                            justifyContent="Center"
                            alignItems="Center"
                            wrap="Wrap"
                            class="flowVisualization">

                        </HBox>


                        <!-- Validation -->
                        <MessageStrip
                            text="Integration flow generated successfully. You can validate it before downloading the SAP artifact."
                            type="Success"
                            showIcon="true"
                            class="sapUiMediumMarginTop"/>


                        <!-- Actions -->
                        <Toolbar class="actionToolbar">

                            <ToolbarSpacer/>

                            <Button
                                text="Validate"
                                icon="sap-icon://validate"
                                press=".onValidate"/>

                            <Button
                                text="Edit Scenario"
                                icon="sap-icon://edit"
                                press=".onEdit"/>

                            <Button
                                text="Download .zip"
                                icon="sap-icon://download"
                                type="Emphasized"
                                press=".onDownload"/>

                        </Toolbar>

                    </VBox>

                </content>

            </Panel>

        </content>

    </Page>

</mvc:View>